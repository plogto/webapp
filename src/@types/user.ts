export type User = {
  id: string;
  username: string;
  fullname: string;
  email: string;
  connectionStatus: number;
  followersCount: number;
  followingCount: number;
  postsCount: number;
  createdAt: string;
  updatedAt: string;
};
